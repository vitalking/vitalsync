VS.Notifications=(()=>{let container;const init=()=>{container=VS.create('div','vs-toast-container');document.body.appendChild(container);VS.events.on('newEvent',onEv)};const onEv=ev=>{if(ev.type==='liquidation'||ev.type==='sync')show({icon:ev.type==='sync'?'var(--vs-accent-primary)':'var(--vs-accent-critical)',text:ev.title+': '+ev.desc})};const show=({icon,text})=>{const t=VS.create('div','vs-toast');const i=VS.create('div','vs-toast__icon');i.style.background=icon;const tx=VS.create('span','vs-toast__text',{text});const tm=VS.create('span','vs-toast__time',{text:VS.formatTime(new Date())});t.appendChild(i);t.appendChild(tx);t.appendChild(tm);container.appendChild(t);while(container.children.length>3)container.removeChild(container.children[0]);setTimeout(()=>{t.classList.add('vs-toast--exiting');setTimeout(()=>t.remove(),200)},4000)};return{init,show}})();
