const VS=window.VS||{};VS.$=(s,p=document)=>p.querySelector(s);VS.$$=(s,p=document)=>[...p.querySelectorAll(s)];VS.create=(tag,cls,attrs={})=>{const el=document.createElement(tag);if(cls)el.className=cls;Object.entries(attrs).forEach(([k,v])=>{if(k==='text')el.textContent=v;else if(k==='html')el.innerHTML=v;else el.setAttribute(k,v)});return el};VS.formatPrice=(p,d=2)=>p.toLocaleString('en-US',{minimumFractionDigits:d,maximumFractionDigits:d});VS.formatPercent=(v,d=2)=>`${v>=0?'+':''}${v.toFixed(d)}%`;VS.formatCompact=v=>{if(Math.abs(v)>=1e9)return(v/1e9).toFixed(1)+'B';if(Math.abs(v)>=1e6)return(v/1e6).toFixed(1)+'M';if(Math.abs(v)>=1e3)return(v/1e3).toFixed(1)+'K';return v.toFixed(0)};VS.lerp=(a,b,t)=>a+(b-a)*t;VS.clamp=(v,mn,mx)=>Math.max(mn,Math.min(mx,v));VS.map=(v,iMin,iMax,oMin,oMax)=>oMin+(oMax-oMin)*((v-iMin)/(iMax-iMin));VS.smoothstep=(e0,e1,x)=>{const t=VS.clamp((x-e0)/(e1-e0),0,1);return t*t*(3-2*t)};VS.random=(mn,mx)=>Math.random()*(mx-mn)+mn;VS.randomInt=(mn,mx)=>Math.floor(VS.random(mn,mx+1));VS.randomChoice=a=>a[Math.floor(Math.random()*a.length)];VS.hexToRgba=(hex,a=1)=>{const r=parseInt(hex.slice(1,3),16),g=parseInt(hex.slice(3,5),16),b=parseInt(hex.slice(5,7),16);return`rgba(${r},${g},${b},${a})`};VS.timeAgo=ts=>{const s=Math.floor((Date.now()-ts)/1000);if(s<60)return s+'s';if(s<3600)return Math.floor(s/60)+'m';if(s<86400)return Math.floor(s/3600)+'h';return Math.floor(s/86400)+'d'};VS.formatTime=d=>d.toLocaleTimeString('en-US',{hour12:false,hour:'2-digit',minute:'2-digit',second:'2-digit'});VS.events={_l:{},on(e,cb){if(!this._l[e])this._l[e]=[];this._l[e].push(cb)},off(e,cb){if(!this._l[e])return;this._l[e]=this._l[e].filter(c=>c!==cb)},emit(e,d){if(!this._l[e])return;this._l[e].forEach(cb=>cb(d))}};VS.animations={_cb:new Map,_running:false,_fId:null,add(id,cb){this._cb.set(id,cb);if(!this._running)this._start()},remove(id){this._cb.delete(id);if(this._cb.size===0)this._stop()},_start(){this._running=true;const loop=ts=>{this._cb.forEach(cb=>cb(ts));if(this._running)this._fId=requestAnimationFrame(loop)};this._fId=requestAnimationFrame(loop)},_stop(){this._running=false;if(this._fId)cancelAnimationFrame(this._fId)}};VS.throttle=(fn,d)=>{let last=0;return(...a)=>{const now=Date.now();if(now-last>=d){last=now;fn(...a)}}};VS.debounce=(fn,d)=>{let t;return(...a)=>{clearTimeout(t);t=setTimeout(()=>fn(...a),d)}};window.VS=VS;
