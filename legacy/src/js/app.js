VS.App=(()=>{const init=()=>{setTimeout(()=>{const loader=VS.$('#loader');if(loader)loader.classList.add('vs-loader--hidden')},1800);setTimeout(()=>{VS.DataEngine.start(100);VS.VisualAlgorithm.init();VS.Header.init();VS.Radar.init();VS.VisualEngine.init();VS.SyncEngine.init();VS.Timeline.init();VS.Notifications.init();VS.Modes.init();console.log('%c VITALSYNC ','background:#00e5ff;color:#08090c;font-weight:bold;padding:4px 8px')},500);document.addEventListener('keydown',e=>{if(e.target.tagName==='INPUT')return;switch(e.key.toLowerCase()){case'e':VS.Modes.setMode('exploration');break;case's':VS.Modes.setMode('sniper');break;case'i':VS.Modes.setMode('institutional');break;case'escape':VS.Modes.setMode('exploration');break}});window.addEventListener('resize',VS.debounce(()=>VS.events.emit('resize',{width:innerWidth,height:innerHeight}),200))};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();return{init}})();
